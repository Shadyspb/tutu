<tr>
  <td><%= car.number %></td>
  <td><%= car.type %></td>
  <td><%= car.train.number %></td>

  <td><%= link_to 'Показать', admin_car_path(car) %></td>
  <td><%= link_to 'Редактировать', admin_edit_car_path(car) %></td>
  <td><%= link_to 'Удалить', admin_car_path(car), method: :delete, data: { confirm: 'Вы уверены?' } %></td>
</tr>
