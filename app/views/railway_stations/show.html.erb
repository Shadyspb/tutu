<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @railway_station.title %>
</p>

<p>
  <% if @railway_station.routes.any? %>
    Станция входит в следующие маршруты:
    <ul>
      <%@railway_station.routes.each do |route| %>
        <li><%=route.name %></li>
      <% end %>
    </ul>
  <% else %>
    Станция не входит не в один маршрут
  <% end %>
</p>

<%= link_to 'Edit', edit_railway_station_path(@railway_station) %> |
<%= link_to 'Back', railway_stations_path %>
